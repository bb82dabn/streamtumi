# Required for production builds. Development defaults to http://localhost:3000.
EXPO_PUBLIC_API_ORIGIN=https://channels.example.com
# Hostname only. Required for production universal and verified links.
EXPO_PUBLIC_APP_HOST=channels.example.com
